/usr/local/nginx/sbin/nginx & 
sudo service apache2 restart & 
while :; do (for file in /home/javucio/channel1/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://nginx/show/stream; done); done &




 while :; do (for file in /home/javucio/channel2/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream2; done); done & while :; do (for file in /home/javucio/channel3/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -map 0:0 -map 0:2 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream3; done); done & while :; do (for file in /home/javucio/channel4/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream4; done); done & while :; do (for file in /home/javucio/channel5/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream5; done); done & while :; do (for file in /home/javucio/channel6/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream6; done); done & while :; do (for file in /home/javucio/channel7/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream7; done); done & while :; do (for file in /home/javucio/channel8/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream8; done); done & while :; do (for file in /home/javucio/channel9/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream9; done); done & while :; do (for file in /home/javucio/channel10/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream10; done); done & while :; do (for file in /home/javucio/channel11/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream11; done); done & while :; do (for file in /home/javucio/channel12/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream12; done); done & while :; do (for file in /home/javucio/channel13/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream13; done); done & while :; do (for file in /home/javucio/channel14/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream14; done); done & while :; do (for file in /home/javucio/channel15/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream15; done); done &while :; do (for file in /home/javucio/channel16/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream16; done); done & while :; do (for file in /home/javucio/channel17/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream17; done); done & while :; do (for file in /home/javucio/channel18/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream18; done); done & while :; do (for file in /home/javucio/channel19/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream19; done); done & while :; do (for file in /home/javucio/channel20/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream20; done); done & while :; do (for file in /home/javucio/channel21/*.*; do ffmpeg -re -i "$file" -vcodec libx264 -vprofile baseline -g 30 -acodec aac -strict -2 -f flv rtmp://localhost/show/stream21; done); done
